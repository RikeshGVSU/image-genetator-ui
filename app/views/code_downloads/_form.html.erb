<%= form_with(model: code_download, local: true) do |form| %>
  <% if code_download.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(code_download.errors.count, "error") %> prohibited this code_download from being saved:</h2>

      <ul>
      <% code_download.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :versions %>
    <%= form.text_field :versions %>
  </div>

  <div class="field">
    <%= form.label :file %>
    <%= form.file_field :file %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
